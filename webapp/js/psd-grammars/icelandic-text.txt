text = txt:[^()-]+ lemma:("-" [^()-]+ )? {
if (lemma) {
    lemma = lemma[1].join("");
    return {
        text: txt.join(""),
        lemma: lemma
    };
} else {
    return {
        text: txt.join("")
    };
}
}
